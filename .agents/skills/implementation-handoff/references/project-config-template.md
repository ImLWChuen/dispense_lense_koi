# Implementation handoff project configuration

## Repository

- Project name: `<project name>`
- Feature branch: `<working branch>`
- Base branch: `<base branch>`
- Sequential shared checkout: `yes | no`
- Task ID prefix: `<PREFIX>`

## Agent responsibilities

- Planner/reviewer: `<agent or role>`
- Implementer/executor: `<agent or role>`
- Human authority: `<person or team responsible for push, PR, and merge decisions>`

## Git policy

- Local commits by implementer: `allowed | disallowed`
- Push: `<explicit policy>`
- Pull requests: `<explicit policy>`
- Merge: `<explicit policy>`
- Commit format: `<type(scope): outcome or repository convention>`
- Task packet commit policy: `<how planner-authored task files enter history>`

## Project responsibilities in scope

Describe the domains this workflow may assign. Record ownership boundaries with other people or agents.

## Architecture and contracts

List authoritative architecture, API, data, and product requirement sources. Distinguish decisions from proposals.

## Required commands

List only commands verified to exist. Include their working directories.

## Sensitive and excluded data

List files, data, credentials, generated output, and environments that must not enter commits.

## Planner escalation boundaries

Record any project-specific decisions the implementer cannot make independently.

