# Implement the next handoff task

Use the `implementation-handoff` skill in implementer mode.

Read `.agents/handoff/PROJECT.md`, `.agents/handoff/QUEUE.md`, the one task marked `ready`, all applicable repository instructions, and the relevant code and tests. Perform every preflight check before editing.

Implement only the task packet's scope, run its required verification, complete its implementation report, and create one atomic local commit when every required acceptance criterion is satisfied. Do not push, merge, create a pull request, modify the base branch, or silently expand architecture, interfaces, database contracts, dependencies, ownership boundaries, or task scope.

If the task cannot be completed within those boundaries, return a blocked report with evidence and the smallest planner decision needed.

